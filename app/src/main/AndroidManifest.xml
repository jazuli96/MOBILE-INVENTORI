<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.utsmoprog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateData" android:label="@string/title_activity_main"></activity>
        <activity android:name=".ViewData" android:label="@string/title_activity_main"></activity>
        <activity android:name=".EditData" android:label="@string/title_activity_main"></activity>
        <activity android:name=".ViewSingleData" android:label="@string/title_activity_main"></activity>
    </application>

</manifest>